(function($) {
  
  $(document).ready(function() {

    $('.menu-item > a').each( function(index, element) {
      // $(this).append("<span class='span-right'>/</span>")
      // $(this).prepend("<span class='span-left'>/</span")
      // totalwidth = 0
      // itemlength = element.offsetWidth
      //   while (totalwidth < 2000) {
      //     var cloned = $(this).clone()
      //     totalwidth += itemlength
      //     cloned.appendTo(element.closest('li'))
      //     cloned.css({'transform':'translateX(' + totalwidth + 'px)'})
      //   }
    })
  })
  
})( jQuery );